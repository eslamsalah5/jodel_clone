import 'package:flutter/material.dart';

class InfoOnBoardingViewBody extends StatelessWidget {
  const InfoOnBoardingViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('hiiii'),
    );
  }
}
