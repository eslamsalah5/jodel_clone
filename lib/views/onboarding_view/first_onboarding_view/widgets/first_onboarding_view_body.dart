import 'package:flutter/material.dart';

class FirstOnBoardingViewBody extends StatelessWidget {
  const FirstOnBoardingViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('First OnBoarding'),
    );
  }
}
